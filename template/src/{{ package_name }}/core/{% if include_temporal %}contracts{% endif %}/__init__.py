"""Cross-boundary SPI — pure pydantic DTOs, Final constants, Protocol ports.

Arrows point inward; import nothing impure. `git mv` this to a workspace member
(packages/<pkg>-contracts/) the day a second REPO must import it — not before.
"""
